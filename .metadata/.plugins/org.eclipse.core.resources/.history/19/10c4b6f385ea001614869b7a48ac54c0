package Game;

import Entity.Entity;
import EntityComponent.GraphicsComponent;
import GUI.UI;
import Movement.FreeForce;
import Movement.Movement;
import Movement.MovementComponent;

public class Game
{
	private Scene scene;

	private Entity player;
	
	private UI ui;
	
	private Updater updater;
		
	public Game()
	{	
		scene = new Scene();
		
		updater = new Updater(60);

		updater.addUpdatable(
				delta -> scene.update(delta));
		
		player = new Entity();
		
		ui = new UI(scene, updater);
		
		
		// TESTING
		
		player.add(new GraphicsComponent(),
				   new MovementComponent());
		
		scene.addEntity(player);
		
		Movement movement = player.get(MovementComponent.class)
							   	  .getMovement();
		
		//movement.add(new FreeForce(100, 0));
		//movement.add(new FreeForce(100, 1));
	}
	
	public void start()
	{
		ui.setGUIVisible(true);
		updater.start();
	}
	
}
